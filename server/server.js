const express = require('express');
const app = express();

// app.get('/api', (req, res) => {
//   res.json({ 'users': ['userOne', 'userTwo', 'userThree'] })
// })
 
// server is on 5000, client is on 3000 because ruby
// app.listen(5000, () => { console.log('Server started on port 5000') })